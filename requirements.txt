# ============================================================================
# Python dependencies for the Real Estate Agent application
#
# Version pinning strategy:
# - Major version locked (e.g., 2.x.x) to prevent breaking changes
# - Minor and patch versions specified for reproducibility
# - Allows pip to pull compatible patch updates (security fixes)
#
# Install with: pip install -r requirements.txt
# ============================================================================

# FastAPI web framework for the REST API
# Features: automatic OpenAPI documentation, async support, request validation
fastapi==0.104.1

# Uvicorn ASGI server to run FastAPI
# [standard] includes uvloop and httptools for performance
uvicorn[standard]==0.24.0

# Pydantic for data validation and serialization
# Used for all request/response schemas (HouseFeatures, AgentResponse, etc.)
pydantic>=2.0.0

# scikit-learn for the ML model
# Includes preprocessing (ColumnTransformer, OneHotEncoder), Ridge, RandomForest, GradientBoosting
scikit-learn==1.3.2

# pandas for data manipulation
# Used to create DataFrames for the ML pipeline.predict()
pandas==2.1.1

# numpy for numerical operations
# Required by scikit-learn and pandas
numpy==1.26.0

# joblib for serializing the sklearn pipeline
# Used to save and load the trained model from disk
joblib==1.3.2

# Streamlit for the web UI
# Simple Python API for building interactive dashboards
streamlit==1.28.1

# python-dotenv for loading environment variables from .env file
# Used to load GEMINI_API_KEY and API_URL configuration
python-dotenv==1.0.0

# Google Generative AI library for Gemini API access
# Used for Stage 1 (extraction) and Stage 2 (interpretation) LLM calls
google-generativeai==0.3.0

# requests library for HTTP calls from Streamlit UI to FastAPI backend
# Used in streamlit_app.py to call /extract and /predict endpoints
requests==2.31.0

# Jupyter and related packages for the notebook
# Used in notebooks/ml_pipeline.ipynb for EDA, model training, prompt testing
jupyter==1.0.0
notebook==7.0.6
matplotlib==3.8.0
seaborn==0.13.0
